import { FilterVehiclePipe } from './filter-vehicle.pipe';

describe('FilterVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
